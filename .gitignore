# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Dependency directories
node_modules/
jspm_packages/
secrets/

# Production build outputs (nếu bạn có quy trình build riêng)
build/
dist/
tmp/
out/

# Editor directories and files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# OS generated files
.DS_Store
Thumbs.db

# Sensitive files (RẤT QUAN TRỌNG)
.env             # Bất kỳ file .env nào
.env.* # Các biến thể của .env (ví dụ: .env.local, .env.development)
*.json           # Cân nhắc nếu bạn có file JSON nhạy cảm khác không phải serviceAccountKey.json
serviceAccountKey.json # Nếu bạn có file này, đảm bảo nó bị bỏ qua