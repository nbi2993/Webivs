# Cấu trúc dự án này được thiết kế để dễ dàng mở rộng, bảo trì và cộng tác.
# Công nghệ: Node.js (Backend) & React Native (Frontend)

#=============================================#
#      BACKEND - NODE.JS / EXPRESS.JS         #
#=============================================#

kinderlink-api/
├── src/
│   ├── api/                  # Chứa toàn bộ logic về routes và controllers
│   │   ├── routes/           # Định nghĩa các đường dẫn (endpoints) của API
│   │   │   ├── auth.route.js # Routes cho xác thực
│   │   │   ├── user.route.js # Routes cho người dùng
│   │   │   └── index.js      # File tổng hợp tất cả các routes
│   │   └── controllers/      # Chứa logic xử lý cho mỗi route
│   │       ├── auth.controller.js
│   │       └── user.controller.js
│   ├── config/               # Chứa các file cấu hình
│   │   ├── config.js         # Cấu hình chung (port, env, database url)
│   │   └── passport.js       # Cấu hình xác thực (JWT strategy)
│   ├── models/               # Định nghĩa cấu trúc dữ liệu (Database Schema)
│   │   ├── user.model.js
│   │   ├── teacher.model.js
│   │   └── child.model.js
│   ├── services/             # Chứa các logic nghiệp vụ phức tạp
│   │   ├── auth.service.js
│   │   └── email.service.js  # Ví dụ: dịch vụ gửi email
│   ├── middlewares/          # Các hàm xử lý trung gian (VD: check auth, validate)
│   │   ├── auth.js
│   │   └── validate.js
│   ├── utils/                # Các hàm tiện ích dùng chung
│   │   └── ApiError.js       # Class để xử lý lỗi API chuẩn
│   ├── app.js                # File chính của ứng dụng Express
│   └── server.js             # File khởi chạy server
├── .env                      # File chứa các biến môi trường (KHÔNG đưa lên Git)
├── package.json              # Quản lý các thư viện và script
└── .gitignore                # Các file/thư mục được bỏ qua bởi Git

#=============================================#
#       FRONTEND - REACT NATIVE               #
#=============================================#

kinderlink-app/
├── src/
│   ├── api/                  # Chứa các hàm gọi API tới backend
│   │   └── client.js         # Cấu hình Axios hoặc Fetch client
│   ├── assets/               # Chứa tài nguyên tĩnh
│   │   ├── fonts/
│   │   └── images/
│   ├── components/           # Chứa các UI component có thể tái sử dụng
│   │   ├── common/           # Components chung (Button, Input, Card)
│   │   └── specific/         # Components cho từng tính năng cụ thể
│   ├── config/               # Cấu hình cho ứng dụng (màu sắc, fonts)
│   │   ├── colors.js
│   │   └── theme.js
│   ├── navigation/           # Định nghĩa luồng di chuyển giữa các màn hình
│   │   ├── AppNavigator.js
│   │   └── AuthNavigator.js
│   ├── screens/              # Chứa các màn hình hoàn chỉnh của ứng dụng
│   │   ├── Auth/             # Màn hình cho luồng xác thực
│   │   │   ├── LoginScreen.js
│   │   │   └── ForgotPasswordScreen.js
│   │   ├── Main/             # Màn hình cho luồng chính
│   │   │   ├── DashboardScreen.js
│   │   │   └── TeacherListScreen.js
│   ├── store/                # Quản lý trạng thái toàn cục (Redux/Zustand)
│   │   ├── slices/           # Các "lát cắt" trạng thái (authSlice, userSlice)
│   │   └── store.js          # File cấu hình store chính
│   ├── utils/                # Các hàm tiện ích
│   │   └── helpers.js
│   └── App.js                # File gốc của ứng dụng React Native
├── app.json                  # Cấu hình của ứng dụng (tên, icon, splash screen)
├── package.json              # Quản lý các thư viện và script
└── .gitignore

